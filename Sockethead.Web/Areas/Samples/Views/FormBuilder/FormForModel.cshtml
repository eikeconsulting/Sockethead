@using Sockethead.Razor.Forms
@model UserProfile

<div class="row">
    <div class="col-md-5">
        <h4>Basic Usage</h4>
        @(await Html
            .SimpleForm(Model, opt => opt.ActionName = "FormForModel")
            .AddRowFor(model => model.First)
            .AddRowFor(model => model.Last)
            .AddRowFor(model => model.JobTitle)
            .SubmitButton("Save Changes")
            .RenderAsync())
        
        <hr/>
        
        @(await Html
            .SimpleForm(Model, opt => opt.ActionName = "FormForModel")
            .AddRowsForModel()
            .SubmitButton("Save Changes")
            .RenderAsync())
        
        
    </div>
    <div class="col-md-7">
        TBD...
    </div>
</div>
