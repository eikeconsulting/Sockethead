@model ExtendedUserProfile

<div class="row">
    <div class="col-md-6">
        @(await Html
            .SimpleForm(Model)
            .RegisterEnums<Gender>()
            .AddRowsForModel()
            .AddSubmitButton("Save Changes")
            .RenderAsync())
    </div>
    <div class="col-md-6">
        <h4>Razor</h4>
        <pre><code>@@(await Html
    .RegisterEnums&lt;Gender>()
    .AddRowsForModel()
    .AddSubmitButtonRow("Save Changes")
    .RenderAsync())</code></pre>
        <p>
        </p>
        Enums can also be automatically generated by registering via the RegisterEnums method, causing  
        them to be rendered as a dropdown lists.  
        This is useful when Enum are mixed in with other properties that you want rendered in the order they are 
        declared in the class.
        <br/><br/>
        This registration is currently limited to three enums.
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
